<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="add-expiry-fields-to-access-requests" author="admin">
        <addColumn tableName="access_requests">
            <column name="expiry_hours" type="int" defaultValue="2160">
                <constraints nullable="false"/>
            </column>
        </addColumn>

        <addColumn tableName="access_requests">
            <column name="expiry_date" type="timestamp" defaultValue="null">
                <constraints nullable="true"/>
            </column>
        </addColumn>

        <!-- Update existing records to have expiry_date based on request_time + expiry_hours -->
        <sql>
            UPDATE access_requests 
            SET expiry_date = DATE_ADD(request_time, INTERVAL expiry_hours HOUR)
            WHERE expiry_date IS NULL;
        </sql>
    </changeSet>

    <changeSet id="add-is-deleted-to-asset-credentials" author="admin">
        <addColumn tableName="asset_credentials">
            <column name="is_deleted" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </addColumn>
    </changeSet>
</databaseChangeLog> 